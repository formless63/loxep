# apps/web development environment.
#
# The server-side auth instance (src/server/auth.ts) builds from Loxep
# bootstrap configuration (ADR-0016) at first use, so `bun run dev` needs the
# LOXEP_* variables below in its environment (e.g. an apps/web/.env file, or
# exported in your shell). The canonical, fully commented reference is the
# repo-root ../../.env.example and the docs' Configuration & Secrets
# "Bootstrap environment reference" — this file only lists what dev needs.

# Required in every mode
LOXEP_DATABASE_URL=postgresql://postgres:loxep-dev@localhost:5433/loxep
LOXEP_KEYRING={"active_version":1,"keys":{"1":"REPLACE-WITH-32-BYTE-BASE64-KEY-FROM-urandom="}}

# Required in web/all modes
LOXEP_PUBLIC_ORIGIN=http://localhost:3020
LOXEP_AUTH_SECRET=replace-with-at-least-32-random-characters

# At least one login path (web/all): SMTP magic-link group and/or OIDC group
LOXEP_SMTP_URL=smtp://localhost:1025
LOXEP_SMTP_FROM=loxep@example.com
#LOXEP_OIDC_ISSUER=https://id.example.com
#LOXEP_OIDC_CLIENT_ID=loxep
#LOXEP_OIDC_CLIENT_SECRET=change-me

# Optional
#LOXEP_MODE=all
#LOXEP_PORT=3020
#LOXEP_BOOTSTRAP_ADMIN_EMAIL=you@example.com
#LOXEP_MEDIA_ROOT=./data/media
#LOXEP_LOG_LEVEL=info
