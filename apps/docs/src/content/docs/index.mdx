---
title: Documentation
description: Architecture, product map, and implementation guidance for Loxep.
sidebar:
  hidden: true
hero:
  title: Loxep
  tagline: Self-hosted marketplace intelligence and business operations.
  actions:
    - text: Architecture overview
      link: ./architecture/system-overview/
    - text: Product map
      link: ./product/master-domain-map/
---

Loxep is an open-source, self-hosted application for observing marketplace activity and operating the commercial work that follows from it. The initial vertical slice is eBay monitoring and historical intelligence, built on a foundation intended to grow into commerce, inventory, projects and services, billing, accounting, tax support, documents, and reporting without turning into disconnected tools.

This site is the project specification as well as the public documentation. During foundation work, it should answer the questions a contributor or coding agent would otherwise have to guess.

## Start here

- [Vision](./overview/vision/) — what Loxep is trying to become and what it is not.
- [Workspaces & Navigation](./product/workspaces/) — the current application-shell model and proposed top-level product workspaces.
- [Master Domain Map](./product/master-domain-map/) — the broader product territory and scope labels.
- [System Overview](./architecture/system-overview/) — runtime, deployment, provider-ingestion, and domain flow.
- [Configuration & Secrets](./architecture/configuration-and-secrets/) — what belongs in environment/bootstrap configuration versus the application database.
- [Phase 0 Foundation](./architecture/phase-0-foundation/) — the capabilities that must exist before marketplace implementation becomes the primary focus.
- [Foundation Schema](./architecture/foundation-schema/) — the current first-migration target.
- [Implementation Contract](./development/implementation-contract/) — load-bearing rules for implementation work and coding agents.
- [Dependency & Version Policy](./development/dependency-policy/) — verify current viable upstream releases before every new pin.

## Current repository state

The web application is no longer a blank planned scaffold. The Kiranism TanStack Start dashboard has been adopted as a UI donor and moved into a Loxep workspace-aware shell:

```text
/dashboard/*    Loxep's real dashboard workspace
/starter/*      preserved donor/demo reference workspace
```

The sidebar and command palette derive their navigation from the active workspace. Future major product areas should be peers of `/dashboard`, not children of one giant `/dashboard/*` navigation tree.

The default runtime direction remains deliberately small:

```text
loxep                    # TanStack Start + Graphile Worker
postgres-timescale       # PostgreSQL + TimescaleDB
rustfs                   # optional S3-compatible companion
```

One Loxep image supports `LOXEP_MODE=all|web|worker`. Local media storage remains the zero-extra-service default; generic S3 storage is supported when shared object storage is desired.

## Documentation rule

Starlight renders each page's frontmatter `title` as the page heading. New documentation pages should therefore **not** repeat the title as a Markdown `# H1`. Use `##` as the first content heading.
