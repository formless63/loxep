---
title: Integrations Status
---

:::caution[Keep this current]
This is the one table every agent and the owner can trust for "what actually works today" across every provider Loxep has adapted, cataloged, wired, or surveyed. **Update this page whenever an integration changes tier** — a new adapter package, a new catalog entry, new runtime wiring (a poll executor, a push task, an on-demand action), or a live-verification result. A stale row here is worse than no row: the next agent to touch this repo is expected to trust it over memory or a design doc's aspirational language. Every cell below is sourced from the repository at HEAD, not from a design doc's stated intent — where a design doc and the code disagree, the code wins and the doc should be corrected (see the [Weave Audit](../weave-audit-2026-08/)'s recurring finding on that pattern).
:::

## What the columns mean

- **Adapter package** — does `packages/integrations/<name>` (or the equivalent) exist and build? A "yes" here means a source-verified boundary adapter with the standard error taxonomy exists; it says nothing about whether anything calls it.
- **Catalog-connectable** — does `apps/web/src/features/settings/integrations-catalog.ts` carry an entry for this provider, with a guided form on `/settings/connections`? A pasted credential is only possible when this is "yes."
- **Runtime-wired** — does anything in `packages/app` or `apps/web/src/server` actually construct the adapter and call it on a schedule or on demand? `poll` = a Graphile Worker poll executor on the shared scheduling model; `push` = an outbound write on a schedule; `on-demand` = a request-scoped server action a human triggers; `none` = the adapter exists but nothing in production code ever constructs it, so a saved credential does nothing visible.
- **Live-verified** — has this integration's mapping been exercised against a real, non-fixture upstream account/instance? "blocked" names the specific precondition (an owner-side credential, app approval, etc.) rather than just saying no.
- **Guide** — the `/guides/connecting-*` walkthrough, if one exists. A guide can exist ahead of the wiring it describes; where that is currently true, the guide itself carries a `:::note` flagging it — this table is the second, redundant place that fact lives, on purpose.
- **Tracking bead** — the open `bd` issue that owns the next step, if any. A blank cell means either nothing remains open (fully shipped) or nothing has been filed (surveyed only, no bead yet).

## Marketplace and commerce providers

| Provider | Adapter package | Catalog-connectable | Runtime-wired | Live-verified | Guide | Tracking bead |
|---|---|---|---|---|---|---|
| eBay | yes — `packages/integrations/ebay` | yes | poll (watchlist, search, seller, `ebay_orders`, `ebay_purchases`) | yes for watchlist/search/seller/notifications (live-verified 2026-08-12 against sandbox); orders mapping is fixture-verified, not yet live; purchases (`WonList`) cannot be sandbox-verified at all — production-account-only | [Connecting eBay](../../guides/connecting-ebay/) | `loxep-62v` (remaining dev-artifact/order-scope step) |
| WooCommerce | yes — `packages/integrations/woo` | yes | poll (`woo_orders`) | yes — exercised against a live production store with read-only credentials | [Connecting WooCommerce](../../guides/connecting-woocommerce/) | — |
| Medusa | yes — `packages/integrations/medusa` | yes | poll (`medusa_orders`) | yes — exercised against a real, throwaway-provisioned Medusa 2.18.0 backend (2026-08-12); the live suite proves facts a fixture cannot: `total_amount` stays `original_total` and does not fall to the refund-reduced `total`, `refunded_amount` matches the summed refunds with nothing double-subtracting, subtotal plus shipping never exceeds the total, fee rows are zero, and a second poll re-delivers the inclusive-boundary order without creating a duplicate | [Connecting Medusa](../../guides/connecting-medusa/) | `loxep-xxz` |
| Etsy | yes — `packages/integrations/etsy` | yes | poll (`etsy_listing`, `etsy_shop`) | blocked — owner must register and get approval for an Etsy Developer Portal app (~24–48h) before any live call, including public reads | [Connecting Etsy](../../guides/connecting-etsy/) | `loxep-g4t.2` (orders), `loxep-g4t.5` (search + non-owned-shop, ToS-gated), `loxep-g4t.4` (listing write) |
| Reverb | yes — `packages/integrations/reverb` | yes | poll (`reverb_listing`, `reverb_shop`) | not stated in shipped docs — Personal Access Token is self-service/instant, but no record of a live-account exercise was found | [Connecting Reverb](../../guides/connecting-reverb/) | — |

## Billing, infrastructure, and mail providers

| Provider | Adapter package | Catalog-connectable | Runtime-wired | Live-verified | Guide | Tracking bead |
|---|---|---|---|---|---|---|
| Invoice Ninja | yes — `packages/integrations/invoiceninja` | yes | on-demand (`pushDraftInvoice` server action) | no — write mapping not independently confirmed against a live push in this environment | [Connecting Invoice Ninja](../../guides/connecting-invoice-ninja/) | — |
| ntfy | n/a — transport lives in `@loxep/notifications`, not `packages/integrations` | yes | push (notification delivery) | yes — delivered end to end 2026-08-12 (real sandbox price-drop event, two delivered pushes) | (settings-level, no dedicated connecting guide) | — |
| Cloudflare | yes — `packages/integrations/cloudflare` | yes | poll/reconcile (`infrastructure_domain_reconcile` task) | blocked — no Cloudflare token exists in this environment yet; every live verification is owner-gated | [Connecting Cloudflare](../../guides/connecting-cloudflare/) | — |
| Purelymail | yes — `packages/integrations/purelymail` | yes | poll/reconcile (mail reconciler tasks) | partially — one live probe established the adapter's DNS-record-set foundation, but no Purelymail API key exists yet for full verification | [Connecting Purelymail](../../guides/connecting-purelymail/) | — |
| Pangolin | yes — `packages/integrations/pangolin` (`loxep-acj.1`, milestone 1: READ ONLY at the adapter boundary; `createRule`/`updateRuleEnabled` land at M4, still no delete verb anywhere in the exported surface, permanently) | yes — catalog entry + guided "Add Pangolin instance" form (`pangolin-api`), with setup guidance leading with the Integration-API-vs-dashboard-URL trap | **read + tier-1 writes + connection health probe + dynamic-IP alias fan-out** — `infrastructure.sync-proxy-resource` (M2, `loxep-acj.2`) reconciles one domain's declared `proxy_resources`; M4 (`loxep-acj.4`) lands the real apply leg for the tier-1 subset (create resource / add target / add rule), gated by M3's (`loxep-acj.3`) `infrastructure.provider_write_policy` tier + the `wouldLockOut` self-lockout preflight — an admin must explicitly raise a connection to `additive` or higher before anything writes, and the resource fronting Loxep itself or Pangolin's own dashboard is refused unconditionally. M5 (`loxep-acj.5`) adds the primitive Pangolin itself does not have: `infrastructure.ip_aliases` (a registered setting, not a table — a rule's stored value is a reference, `alias:<name>`, resolved to a literal only at materialization, refusing loudly on an unresolvable alias) and `infrastructure.detect-ip-aliases` (`packages/app/src/ip-alias-detection.ts`), a 15-minute cron sweep that runs a `dns`/`pangolin_site` detector per alias, fans out to every referencing rule ACROSS every domain and hosting target (`ProxyResourcesService.listRulesReferencingAlias`), and — only when the alias's own `autoApply` flag AND the connection's write-policy tier both permit it — applies the ADD half of add-then-retire via the SAME tier-1 `create-rule` path M4 already ships (retirement stays `loxep-acj.7`'s, tier 3, unimplemented). One `ip_alias_changed` infrastructure-class notification event fires per genuine change, never per sweep. Pangolin also joined `packages/app/src/fleet-health.ts`'s five-sibling connection-probe recipe this milestone (`adapter.listOrgs()` proving the credential) — despite being a control-plane provider, not a fleet companion tool, it had no connection health probe at all before this, so a saved credential read `unknown` forever. | partially, and the finding matters: a 2026-08-15 live reconnaissance run against the owner's real instance (`pangolin.example.com`) confirmed the `{data,success,error,message,status}` envelope shape live (via the dashboard's own sibling `/api/v1` route, HTTP 401 with the exact predicted body) but could NOT reach the standalone Integration API server itself — its port is not exposed on the public origin, any of five plausible dedicated-subdomain guesses, or a confirmed direct Tailscale connection to the actual host. `test/live-pangolin.test.ts` records the outcome as a `not_found` `PangolinAdapterError` (HTTP 404 from the dashboard's own catch-all) rather than crashing, and will start exercising real reads with no code change once the operator adds a working reverse-proxy route for the Integration API's port. Every milestone through M5 ships with NO live write leg (the Integration API remains unreachable from this build network) — every test drives a fake `ProxyProviderPort`/adapter, noted honestly rather than silently; M5's `pangolin_site` alias detector is UNVERIFIED for the identical reason (`PangolinSiteFact.endpoint`'s own shape is unconfirmed against a live read) and degrades to "no detection this round" on any failure rather than guessing | [Connecting Pangolin](../../guides/connecting-pangolin/) | `loxep-acj.6` (M6: the provisioning-template engine), `loxep-acj.7` (M7: retirement by disable — the reversible half of add-then-retire) |

## Fleet observability providers

| Provider | Adapter package | Catalog-connectable | Runtime-wired | Live-verified | Guide | Tracking bead |
|---|---|---|---|---|---|---|
| Tailscale | yes — `packages/integrations/tailscale` | yes | probe + discovery — `health.sweep` dispatches Tailscale connections to `createTailscaleAdapter` through the fleet-aware `connection` health-subject registry (`createFleetHealthSubjectRegistry`, `packages/app/src/fleet-health.ts`); the probe now calls `adapter.listDevices()` (never a separate `probe()` call — the same bulk read proves the credential AND is the discovery read), is the sole writer of `connections.last_success_at`/`last_error_at`/`last_error_code` for this provider, and returns `degraded` ahead of a recorded API-access-token expiry rather than waiting for the token to fail outright. Since `loxep-50t` Slice B, that same successful read ALSO upserts one `external_resources` row per device (`external_id` = the device's `nodeId`, never `name`/`hostname` — neither is contracted unique) and writes a per-device `integration_health` row for LINKED devices only (a tailnet holds laptops and phones nothing at Loxep should watch uninvited). An operator-confirmed attach picker links a discovered device to a hosting target; the fleet-detail page renders a "Private network" row (tailnet address, online/last-seen, the read clock's own age) for an attached device, including a conditional, evidence-withdrawn sentence explaining why some OTHER linked tool on the same host reads `unknown`. No poll executor — there is nothing to sync, only a credential to prove. | **yes** — 2026-08-15 against the owner's real tailnet (read-only `listDevices()`/`probe()`): 27 devices observed, 19 online, 0 unauthorized (counts only, per this repo's "never log device names/addresses" rule for this provider) | [Connecting Tailscale](../../guides/connecting-tailscale/) | `loxep-50t` (Slice B shipped 2026-08-15; Slice C — the tailnet-wide unmatched-device candidates panel on the fleet LIST page — remains open. The per-target attach picker already lists a connection's unattached devices, so Slice C is additive, not blocking) |
| Termix | yes — `packages/integrations/termix` | yes | probe + discovery — same registry dispatches to `createTermixAdapter`'s `probe()` (`POST /users/login` + `GET /users/me`, Termix's only credential-proving call); host/session counts enrich `detail` best-effort and never affect `status`. Since `loxep-wvm` Slice B, a successful `listHosts()` read ALSO upserts one `external_resources` row per host (keyed on `String(id)`, never a name join — Termix's host names carry no uniqueness promise), and a per-host `integration_health` row is written for LINKED hosts only, narrower than every sibling provider. An operator-confirmed attach picker links a discovered host to a hosting target; a best-effort `listSessions()` read (its own call, never a second `listHosts()`) folds an active-session count into each linked host's `detail`. **Per-session rows shipped 2026-08-15 (`loxep-4ah`, owner ruling 6b):** a linked host's Fleet detail page now shows a live-read (never persisted) panel of who is connected, whether it is the caller's own session or one shared to them, permission level, and age — the design's own §3.3(a) gate, resolved. No poll executor. | **yes** — 2026-08-14 against the owner's instance, with a local non-admin account. Observed `{ name, ip, lastSeenAt }` present and `online` **null**, confirming the design's warning that `/status` is the weakest-provenance signal in the fleet. Login limiter behaviour deliberately NOT exercised (that would mean failing logins against a live account). **Discovery itself LIVE-VERIFIED 2026-08-15** (`packages/app/test/live-fleet-discovery.test.ts`, one bounded `listHosts()` read, no second call): the connection probe returned `ok`, every discovered host produced a well-formed `external_resources` row (`provider='termix'`, `externalType='host'`, non-null `externalId`), and — since nothing was attached in that read-only run — zero of them carried a per-resource health row, confirming the "LINKED hosts only" write gate rather than assuming it | [Connecting Termix](../../guides/connecting-termix/) | — (Slice B shipped; Slice C — feeding the shared unmatched-candidates panel — remains open, tracked on `loxep-wvm`) |
| Gatus (read) | yes — `packages/integrations/gatus` | yes | probe + discovery — same registry dispatches to `createGatusAdapter`'s three-posture probe (`probeConfig()`, then `listEndpointStatuses()` in `open`/`basic` posture or `health()` in `oidc` posture), plus a heartbeat mirror that does one extra unauthenticated GET to detect a `gatusPushSetting.endpointKey` mismatch with the outward push (previously a silent no-op). Since `loxep-1au` Slice B, a successful `direct`-posture `listEndpointStatuses()` read ALSO upserts one `external_resources` row per endpoint (`external_id` = Gatus's own raw `key`, never split — the group/name join Gatus performs to build it is lossy and non-injective) and writes a per-endpoint `integration_health` row for LINKED endpoints only. The endpoint named by `gatusPushSetting.endpointKey` is EXCLUDED from discovery entirely, in every posture, regardless of whether the push is enabled — a health row derived from that endpoint's own status would close a self-latching loop with the outward push (Gatus says down → Loxep writes failing → the push reports failure → the endpoint stays down). No unlinked-endpoint count or badge anywhere — the unmapped majority of endpoints is the normal, permanent case, not a backlog. OIDC posture never discovers (the bulk read is unwinnable there). No poll executor. | **yes** — 2026-08-14 (posture/health/config probes) and 2026-08-15 (discovery leg) against the owner's instance: unauthenticated health and config probes answer with the documented shapes, statuses read cleanly (5 endpoints, 0 failing, `open` posture), one probe per `capabilities()` call. Only ONE posture was exercised, so the three-way `open`/`basic`/`oidc` inference stays copy-only. No endpoint's key/group matched a "loxep" heuristic on this run, so verification item 1 (do external-endpoints appear in `GET /api/v1/endpoints/statuses`) was not confirmable against this particular instance's current endpoint set — the discovery/quarantine mechanics themselves are proven against injected adapter fakes, per this repo's "never run live tests repeatedly" rule | [Connecting Gatus](../../guides/connecting-gatus/) | `loxep-1au` (Slice B shipped 2026-08-15; Slice C — the witness fleet panel and the derived cross-tool sentence's Gatus refinement — and Slice D — the `managed_domain` leg — remain open) |
| Gatus (push) | n/a — outbound-only job, no inbound adapter needed | n/a (config lives on `/settings/application`) | push — `gatus-push.ts`'s scheduled task, registered in `registry.ts`. `mode: 'single'` (PROVISIONAL default) pushes one rollup to `endpointKey`, unchanged since `loxep-ovj.2`. `mode: 'facts'` (owner-approved 2026-08-15, `loxep-4ah` ruling 6b) pushes OQ9's five candidate facts — worker backlog, order-sync freshness, notification delivery success, reconciler drift, database-reachability readiness — each to its own key DERIVED from `endpointKey` (`deriveGatusPushFactKey`). Same shared bearer token for all five. A fact whose computation throws is skipped that cycle, never a fabricated failure. | not stated (owner-gated on the operator's own Gatus YAML) | [Publishing health to Gatus](../../guides/gatus-health-push/) | Open question 9 fully resolved (`loxep-4ah`) — see [Fleet Observability Design](../../architecture/fleet-observability-design/#open-questions) |
| Beszel | yes — `packages/integrations/beszel` (`loxep-9j6`, 48 tests) | yes — catalog entry + guided "Beszel readonly user" form (`beszel-login`) | probe + discovery — `health.sweep` calls `adapter.health()` then `adapter.listSystems()` (the credential-proving, discovery-yielding read) via `createBeszelAdapterFactory`, and — since `loxep-y64` slice 3 — that same successful read ALSO upserts one `external_resources` row per system and one per-system `integration_health` row (`subject_type='external_resource'`, `source='adapter'`). An operator-confirmed attach picker on `/infrastructure/fleet/$name` links a discovered system to a host; `diagnoseHostWitnesses` consumes the linked system's real status. No poll executor. | **yes** — 2026-08-14 against the owner's hub (connection-level probe and field discovery). All four fields the adapter called UNVERIFIED (`name`, `host`, `port`, `updated`) are actually sent, and it authenticated as an ordinary readonly user rather than a superuser. One hub is not a schema guarantee, so the defensive parsing stays. The per-system discovery/attach/witness path (`loxep-y64` slice 3) is proven only against injected adapter fakes — no live re-verification was run for it, per this repo's "never run live tests repeatedly" rule | [Connecting Beszel](../../guides/connecting-beszel/) | — (all three `loxep-y64` slices shipped; `loxep-uhs` and `loxep-ovj.5` closed) |
| Dockhand | yes — `packages/integrations/dockhand` (`loxep-9j6`) | yes — catalog entry + guided "Dockhand login" form (`dockhand-login`) | probe + discovery + live containers panel — `health.sweep` calls `adapter.probeSession()` then `adapter.listHosts()` via `createDockhandAdapterFactory`; since `loxep-hb7` Milestone B, that same successful read ALSO upserts one `external_resources` row per environment and a per-environment `integration_health` row, auto-linking to a same-named hosting target on first match (Dockhand is the one fleet provider whose discovery auto-attaches — both sides guarantee name uniqueness) and self-retiring onto the link's id from then on. `/infrastructure/fleet/$name` renders a live, request-scoped Containers/Stacks panel (never persisted) for a linked host via `apps/web`'s own Dockhand adapter access. **Milestone C (host-registration write leg) and Milestone D (drift cadence, adopt) shipped 2026-08-15:** `planContainerHostOperations`'s first non-test caller is `@loxep/infrastructure`'s `container-hosts.ts`, wired to the real adapter by `@loxep/app`'s `infrastructure.reconcile-container-host` task; the create dialog and the fleet-detail "Container host registration" panel both declare intent (write-only TLS/Hawser material in `application_secrets`, never reveal-once — operator-supplied); a stuck `provider_operations` create resolves via a fresh `readHosts()` name match; Milestone D's check-mode drift pass piggybacks the SAME `listHosts()` read `health.sweep` already makes (no second call, no new cron) and an `/infrastructure/overview` card offers "adopt as hosting target" for a Dockhand environment `listUnattachedByProvider` shows unmatched. | **yes** — 2026-08-14, and it found a real bug: the session cookie is `dockhand_session`, not the transcribed `session`, so no live login could ever have worked while 62 stub tests passed. Environment, container AND stack schemas all now observed against a real instance (33 containers, 18 stacks), with presence checked across every returned row rather than the first. **Discovery itself LIVE-VERIFIED 2026-08-15** (`packages/app/test/live-fleet-discovery.test.ts`, one bounded `listHosts()` read, no second call): the connection probe returned `ok`, every discovered environment produced a well-formed `external_resources` row with its own per-resource `integration_health` row (`source='adapter'`). The Containers-panel live read and the exact-name auto-attach path were NOT exercised in this run (no matching hosting target existed in the scratch database) and remain proven only against injected fakes | [Connecting Dockhand](../../guides/connecting-dockhand/) | — (Milestones A–D all shipped on `loxep-hb7`) |

### Link-only tools, removed from the app (owner decision, 2026-08-14)

Cockpit, Netdata and Uptime Kuma were previously listed here as tier-1/tier-2 link-only tools. They are no longer represented in the application at all — their entries were removed from the fleet-tool registry and the tier-2 probe. The owner's rule: *"remove the link-only stuff from within the app. If it doesn't integrate we don't mention it."*

They remain surveyed in the [Fleet Observability Design](../../architecture/fleet-observability-design/)'s per-tool verdicts, because a recorded decision *not* to integrate is worth keeping. What changed is that the product no longer advertises a tool it cannot read. `loxep-ovj.6`, which scoped their registry entries and guides, is closed as won't-do.

## Surveyed only — no adapter, no code

These were researched for a companion-integration design but nothing has been built. "Tracking bead" is blank where no bead exists yet, which is the expected state for a surveyed-only entry, not a gap.

| Provider | Adapter package | Catalog-connectable | Runtime-wired | Live-verified | Guide | Tracking bead |
|---|---|---|---|---|---|---|
| BookStack | no | no | none | n/a | none | `loxep-juk` (P4, blocked on owner pick vs. Outline) |
| Vikunja | no | no | none | n/a | none | `loxep-1wx` (P4) |
| Outline | no | no | none | n/a | none | `loxep-p1j` (P4, qualified — BUSL but self-host use permitted; blocked on owner pick vs. BookStack) |
| AFFiNE | no — **overturned**: no content API exists to build against (metadata/permissions only; content is a Yjs CRDT with no stable external surface), and the self-hostable server requires an Enterprise-license seat | no | none | n/a | none | none — recommendation is to stop carrying it as a pending evaluation |
| Alexandrie | no — watch-tier: no published API endpoint documentation found despite MIT license and RustFS-native storage fit | no | none | n/a | none | none |
| Databasus | no — guidance-doc concept only | no | none | n/a | none | `loxep-7kt` (backup deployment guide; its evidence webhook rides `loxep-ovj.7`) |
| Backrest / restic | no — guidance-doc concept only | no | none | n/a | none | `loxep-7kt` |

## Notes on cells that might look wrong but aren't

- **The fleet inversion `loxep-rf4` was filed against is closed for connection-level health.** Beszel and Dockhand now have catalog entries and guided forms, and all five fleet providers (Beszel, Dockhand, Gatus, Tailscale, Termix) get a real connection health probe from `health.sweep` (`createFleetHealthSubjectRegistry`, `packages/app/src/fleet-health.ts`) that proves the credential and writes `connections.last_success_at`/`last_error_at` — a pasted fleet credential no longer sits behind a permanent `unknown (never succeeded)`. **Beszel's per-resource discovery/linking/health leg closed first (`loxep-y64`, all three slices, 2026-08-15), and every other provider followed the same slice, same day** (`loxep-hb7` Milestone B for Dockhand, `loxep-wvm` Slice B for Termix, `loxep-50t` Slice B for Tailscale, `loxep-1au` Slice B for Gatus): each provider's connection probe now also upserts one `external_resources` row per discovered system/environment/host/device/endpoint and writes a per-resource `integration_health` row (`subject_type='external_resource'`, `source='adapter'`) that `diagnoseHostWitnesses` consumes with no code change of its own. All five providers now carry `healthPath: null` in `fleet-tool-registry.ts` (some because a richer adapter read supersedes it, Tailscale/Termix because no unauthenticated route ever existed), so the generic credential-free tier-2 `external_resource` probe is now permanently vestigial across the whole fleet — `PROBEABLE_FLEET_TOOL_PROVIDERS` is genuinely empty. The providers differ in how a resource gets LINKED: Beszel, Termix, Tailscale, and Gatus require an operator-confirmed attach picker (Tailscale and Gatus additionally upsert `external_resources` for EVERY observed object, whether linked or not, but gate the `integration_health` WRITE on link state — narrower than Beszel's "every discovered object gets a row"); Dockhand auto-links on an exact name match (the one provider where both sides guarantee uniqueness) and self-retires onto the link's id from then on. Dockhand additionally grew the fleet design's one dedicated tool-specific panel — a live, request-scoped Containers/Stacks read on `/infrastructure/fleet/$name`, never persisted; Tailscale grew the "Private network" row on the same page. **What is still open:** Termix's Slice C and Tailscale's Slice C (each provider's own unmatched-candidates panel), and Gatus's Slice C (witness panel) / Slice D (`managed_domain` leg). Dockhand's own Milestones C (host-registration write leg) and D (drift cadence, adopt) closed 2026-08-15 — see the row above.
- **Reverb is the only marketplace adapter with genuine runtime parity to eBay's core loop** (poll executor wired, notifications flow through the same bridge) despite being the newest and smallest of the three marketplace integrations.
- **Medusa's persistence leg has landed.** The adapter, the `@loxep/commerce` translator, the `medusa_orders` monitor target type, and the poll executor all exist and are wired — WooCommerce, eBay, and Medusa now share the same Phase 3 commerce ingestion shape. What remains open for Medusa is the same "later slice" every provider shares: no dedicated `/commerce/orders` surface exists yet for any provider (see the [Weave Audit](../weave-audit-2026-08/), finding 7).
- **ntfy has no adapter package** because it is not a marketplace/fleet-style provider with a request/response API to wrap — it is the notification transport itself, implemented directly in `@loxep/notifications`.

## See also

- [Weave Audit (2026-08)](../weave-audit-2026-08/) — the whole-system finding this page's fleet-provider rows are drawn from (finding 4).
- [Fleet Observability Design (Phase 8)](../../architecture/fleet-observability-design/) — the per-tool tier verdicts behind the fleet rows.
- [Etsy Integration Design](../../architecture/etsy-integration-design/) — the marketplace landscape survey behind the Etsy row.
- [Reverb Integration Design](../../architecture/reverb-integration-design/) — the design behind the Reverb row.
- [Knowledge & Tasks Integration Design](../../architecture/knowledge-tasks-integration-design/) — the survey behind the "surveyed only" table.
